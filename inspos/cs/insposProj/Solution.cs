namespace insposProj
{
	public class Solution
	{
		public int SearchInsert(int[] nums, int target)
		{
			// if (nums.Length == 1) return nums[0] < target ? 1 : 0;
			// int centerId = nums.Length / 2;
			// int leftFromCenterId = centerId - 1;
			// while (!(target >= nums[leftFromCenterId] && target <= nums[centerId]))
			// {
			//	//idk how to solve it.
			// } 
			// for ( ; ; )
			// {
			// 	if (nums[centerId] > target)
			// 	{
			// 		centerId /= 2;
			// 		if (nums[centerId] < target)
			// 		continue;
			// 	}
			// 	else if (nums[centerId] < target)
			// 	{
			// 		centerId += centerId / 2;
			// 		continue;
			// 	}
			// }
		}
	}
}
